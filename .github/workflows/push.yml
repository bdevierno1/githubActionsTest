on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set up Python 2.7
      uses: actions/setup-python@v1
      with:
        python-version: 2.7
    - name: Install dependencies
      run: |
        python --version
